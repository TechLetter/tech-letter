[project]
name = "techletter-content-service"
version = "0.1.0"
description = "Content service for Tech-Letter (Python, FastAPI)."
requires-python = ">=3.11,<3.12"
dependencies = [
  "techletter-common",
  "fastapi",
  "uvicorn[standard]",
]

[project.scripts]
content-service = "app.main:main"
