MONGO_URI="mongodb://example_user:example_pw@localhost:27017/techletter?authSource=admin"

# Kafka 공통 설정
# KAFKA_BOOTSTRAP_SERVERS="kafka:9092"
# KAFKA_GROUP_ID="tech-letter"  # 로컬 실행 기본 consumer group id
# KAFKA_MESSAGE_MAX_BYTES="10485760"      # 10MB
# KAFKA_MAX_POLL_INTERVAL_MS="900000"    # 15분

# 공통 로그 레벨 (Go/Python 모두 사용)
# LOG_LEVEL="INFO"

# Summary Worker LLM 설정
SUMMARY_WORKER_LLM_API_KEY="xxxxxxxxxxxxxxxxxxx"  # google / openai / openrouter 사용 시 필수
# SUMMARY_WORKER_LLM_PROVIDER="google"
# SUMMARY_WORKER_LLM_MODEL_NAME="gemini-2.5-flash"
# SUMMARY_WORKER_LLM_TEMPERATURE="1.0"
# SUMMARY_WORKER_LLM_MAX_RETRIES="0"  # LLM API 실패 시 재시도 횟수 (기본 0)
# SUMMARY_WORKER_LLM_BASE_URL="https://openrouter.ai/api/v1"  # openrouter 사용 시 예시 (ollama 사용 시 미설정하면 http://localhost:11434)

# Google OAuth for API Gateway
GOOGLE_OAUTH_CLIENT_ID="your-google-client-id"
GOOGLE_OAUTH_CLIENT_SECRET="your-google-client-secret"
# GOOGLE_OAUTH_REDIRECT_URL="http://localhost:8080/api/v1/auth/google/callback"

# JWT settings for API Gateway (HS256)
# JWT_SECRET 는 HS256 서명에 사용할 시크릿 문자열이다.
# 예: openssl rand -base64 32
JWT_SECRET="your-jwt-secret"
# JWT_ISSUER="tech-letter"

# OAuth login success redirect
# AUTH_LOGIN_SUCCESS_REDIRECT_URL="https://localhost:5173/login/success"

# CORS allowed origins for API Gateway (comma-separated)
# CORS_ALLOWED_ORIGINS="http://localhost:5173,https://tech-letter.duckdns.org"